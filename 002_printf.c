//002:printf() used to print text on console

#include <stdio.h>

int main()
{

    // by default printf() function cursor is not going in new line means cursor will available after printed text 
    printf("hello amit");
    printf("by default printf have no default line ");

    // /n used for new line 
    printf("upper line \n lower line");
    printf("new line after this \n");
    printf("after line code \n");
    return 0;
}